# Fixing Plan — Deep Layers

Scope: action plan for the ranked findings surfaced by `theory-links.md` §8.4-8.10 (course-lecture-to-code review), plus two loss-function findings (#9, #10) surfaced afterward from a direct question about loss correctness, a paper in `biblio/`, and a colleague's review. Each finding is restated with its priority, the type of change it requires, which checkpoints it forces a retrain of, and the reasoning behind the sequencing chosen to minimize the number of full retrain passes.

---

## 0. Implementation status

- **Round 0 (pilot)** — executed on branch `project-fixing`, `notebooks/P0_groupnorm_pilot.ipynb`. Result: `unet` improved on every metric with GroupNorm (loss 0.2127→0.1964, ssim 0.549→0.585, psnr 18.64→19.35); `unet_v2` regressed marginally (loss 0.1944→0.1966, ssim 0.592→0.586) — no catastrophic instability in either case, so Round 1 proceeded as planned. Baseline (BatchNorm) checkpoints backed up in `models/pf1/` before any Round 1 change.
- **Round 1 — code and notebooks implemented, retraining in progress.** Every change below (#1, #2, #3, #5, #9, #10, §5 callbacks) is live in `scripts/` and `notebooks/020`–`023`/`030`–`032`/`041`/`042`/`051`–`053`; 235/235 unit tests pass. First retraining attempt hit a tensorflow-metal GPU bug (see §7) that has since been worked around across all 9 architecture files — `020_training.ipynb` is being rerun under the fix.
  - Finding #5 turned out not to change model behavior as originally conceived: verified that every `resunet`/`resunet_nll` block always changes channel count (one block per depth level, widening on the way down), so a conditional identity shortcut never fires — kept as unconditional projection, documented in the code. Fixing this for real would need a structural change (a second, same-width block per level) outside this finding's scope.
  - `efficientnet_unet`/`efficientnet_unet_nll` are untouched by Round 1 — they kept the pre-Round-1 recipe (`combined_loss_advanced`, both `gaussian_nll`/`beta_nll`) until Round 2, now consolidated into `022_training_efficientnet.ipynb`/`032_evaluation_efficientnet.ipynb`.
- **Round 2 — EfficientNet, code and notebooks implemented, retraining not yet run.** Every change in §2's Round 2 section (#1/#3 decoder-only, #2, #6 two-phase fine-tuning, #9, #10, plus the §7 NaN-fix parity item) is live in `scripts/` and `notebooks/020`/`021`/`022`/`023`/`030`–`032`/`041`/`042`/`051`–`053`; 236/236 unit tests pass.
  - `scripts/efficientnet_unet.py`'s `_conv_block` (the custom **decoder** only — imported directly by `efficientnet_unet_nll.py`, so one edit covers both architectures) now applies `GroupNormalization`/He init/`_ReLUFix`, mirroring `scripts.unet._conv_block`. The pretrained EfficientNetB0 **encoder** itself is untouched — its internal BatchNorm/ReLU/Swish stay exactly as Keras Applications ships them, since patching pretrained batch statistics would defeat transfer learning (`fixing.md` §2/§7's caveat, unchanged).
  - #9: `scripts.trainer.compile_model` no longer special-cases `efficientnet_unet` — `_ADVANCED_LOSS_ARCHS`/`uses_advanced_loss` were removed as a dead mechanism now that every deterministic architecture (`efficientnet_unet`/`efficientnet_unet_ft` included) trains with the same unified `combined_loss`. `combined_loss_advanced` itself is untouched and still unit-tested in `scripts/losses.py` — just no longer selected by any architecture's default training path. `tests/unit/test_trainer.py` updated accordingly (parametrized `test_compile_model_uses_combined_loss_for_every_architecture` replaces the old advanced-loss-specific tests).
  - #10: `efficientnet_unet_nll`/`efficientnet_unet_nll_ft` now default to `laplace_nll_loss` like every other NLL architecture, closing the `gaussian_nll`/`beta_nll` split project-wide. `scripts/calibration.py`'s `distribution=` parameter (and its `"gaussian"` default) were kept rather than removed: `tests/unit/test_calibration.py` exercises both branches directly as generic, reusable infrastructure, and the Gaussian branch remains the correct way to score any pre-Round-2 checkpoint someone still has on disk — only the stale "until Round 2"/"efficientnet_unet_nll" docstring references pointing at it were updated. Every notebook's `distributions`/`nll_distributions` per-model dispatch dict (`041`, `042`, `051`, `052`, `053`) was simplified away, since `laplace_sigma_from_scale` now applies uniformly to every loaded NLL checkpoint.
  - #6 (two-phase fine-tuning, previously unused): `scripts/train_single.py` gained `--init-from <checkpoint>` (loads weights via a plain `model.load_weights(...)` before compiling — verified sufficient since `scripts.trainer.load_model`'s own load/recompile split already establishes that architecture and compiled state are independent) and `--lr` (so phase 2 can use a different learning rate than `settings.LEARNING_RATE`). `scripts.trainer._BUILDERS`/`scripts.trainer_nll._BUILDERS_NLL` gained `"efficientnet_unet_ft"`/`"efficientnet_unet_nll_ft"` entries (same builders as their phase-1 counterparts), so `get_callbacks`/`load_model`/`load_model_nll` give the fine-tuned checkpoints their own directories (`models/deterministic/efficientnet_unet_ft/`, `models/nll/efficientnet_unet_nll_ft/`) purely by keying off `arch_name`, no other special-casing needed. `022_training_efficientnet.ipynb` orchestrates both phases as two sequential `train_single.py` subprocesses per model family (`fixing.md` §7.1's subprocess-per-architecture pattern, now covering EfficientNet too — closing the last architecture family that still trained in an in-process loop).
    - **Ambiguous point, resolved**: the plan's findings table (row #6) listed `efficientnet_unet_nll` as in scope for two-phase fine-tuning, but §2's detailed Round 2 paragraph only spelled out the checkpoint-naming resolution for the deterministic `efficientnet_unet_ft`. Implemented symmetrically for both — `efficientnet_unet_nll_ft` exists alongside `efficientnet_unet_ft` — since `--init-from`/the `_BUILDERS` entry mechanism made the NLL sibling a small marginal addition once built for the deterministic side, not a second design decision.
    - New `scripts/config.py` fields: `FINETUNE_LEARNING_RATE: float = 1e-5` (10x below `LEARNING_RATE`, a conservative pick within the conventional 10-100x-smaller range for fine-tuning a pretrained encoder, erring toward preserving ImageNet features over faster convergence) and `FINETUNE_EPOCHS: int = 20` (phase 2 refines an already-converged decoder rather than training from scratch, so it gets a much shorter budget than `EPOCHS`; `EARLY_STOPPING_PATIENCE` still governs the actual stopping point, this is only the upper bound).
  - §7 NaN-fix parity (flagged as not-yet-done): `022_training_efficientnet.ipynb` now trains through `scripts/train_single.py` subprocesses like every other training notebook, so `_ReLUFix`/`Adam(clipvalue=...)` apply automatically via `compile_model`/`compile_model_nll` — no separate fix needed, just closing the gap in which notebook actually reaches them.
  - `030_evaluation.ipynb`/`031_evaluation_nll.ipynb`/`020_training.ipynb`/`021_training_nll.ipynb`/`023_training_variants.ipynb` companion-notebook tables and cross-references updated to describe `032_evaluation_efficientnet.ipynb`'s new (post-Round-2) content instead of the old Gaussian/beta-dual-variant description.
  - **Not yet done**: actual retraining under this recipe (all four EfficientNet-based checkpoints still reflect whatever was last trained under the pre-Round-2 code).
  - **Retrained and evaluated**: `efficientnet_unet_ft`/`efficientnet_unet_nll_ft` consistently *underperform* their frozen-encoder counterparts — worse loss/mae/psnr/ssim (`032`), worse-or-mixed AUROC (`040`/`041`), worse stroke coherence on both the 3-image GT set and the full test set (`040`/`041`/`050`/`051`). Same sign across 4+ independent notebooks. **Investigated no further, by explicit user decision** (2026-08-26): attributed to the training set being too small to support full end-to-end fine-tuning of a pretrained encoder without overfitting/forgetting the ImageNet features, rather than a bug in the two-phase mechanism itself. `settings.FINETUNE_LEARNING_RATE`/`FINETUNE_EPOCHS` left as-is; `_ft` checkpoints kept on disk but not treated as an improvement.
- **Round 3 — dilated convolution, implemented, trained, evaluated. Result: negative — closed.** `unet_dilated`/`unet_v2_dilated` (`scripts/unet_dilated.py`/`scripts/unet_v2_dilated.py`) replace the plain bottleneck with `scripts.aspp.dilated_bottleneck` — a multi-scale ASPP-style block (parallel `Conv2D(dilation_rate=r)` branches at `settings.DILATION_RATES = [1, 2, 4, 8]`, concatenated and projected back to `bottleneck` channels with a `1x1` conv), leaving the rest of the encoder/decoder (including all downsampling) unchanged. 250/250 unit tests pass (`tests/unit/test_aspp.py`, `tests/unit/test_unet_dilated.py`, plus `test_trainer.py` registration checks).
  - **Design decision** (user-confirmed before implementation, since #7's spec only said "new variants... with `dilation_rate` on selected conv layers", no further detail): dilation applied **only at the bottleneck**, not by removing/replacing any encoder downsampling stage. The alternative — trading a pooling stage for dilation to preserve more spatial resolution near the bottleneck, closer to the "position loss" tension `theory-links.md` §3.4 actually describes — would need the skip-connection/decoder scheme redesigned and was judged too invasive for an exploratory round; kept as a possible future direction if the bottleneck-only variant doesn't move the needle (it didn't — see below).
  - `unet_v2_dilated` is trained with the same `use_strided_conv=True`/`use_upsample_conv=True`/`dropout_rate=0.2` kwargs `023_training_variants.ipynb` already uses for `unet_v2`, so the only difference within each pair (`unet` vs. `unet_dilated`, `unet_v2` vs. `unet_v2_dilated`) is the bottleneck.
  - `scripts.trainer._BUILDERS` gained `"unet_dilated"`/`"unet_v2_dilated"` entries; both use the same unified `combined_loss` as every other deterministic architecture — no NLL counterparts (not in #7's scope, unlike Round 2's `_ft`/`_nll_ft` symmetry decision).
  - `023_training_variants.ipynb` §3 trains all four variants (`unet_v2`, `unet_restormer`, `unet_dilated`, `unet_v2_dilated`) as four sequential `train_single.py` subprocesses. `033_evaluation_variants.ipynb` (the dedicated `unet`-vs-variants notebook, mirroring `032`'s role for EfficientNet) extended to all four; `040`/`042`/`050` had their previously-commented `unet_v2`/`unet_restormer` entries uncommented and the two dilated variants added (`030`/`031` deliberately still exclude all four, by the same "base architectures only" design already established for `unet_v2`/`unet_restormer`/`efficientnet_unet_ft`). `052`/`053` (user-chosen-subset notebooks) got the two dilated variants added as additional commented `MODELS` entries, not enabled by default.
  - **Trained and evaluated. No measurable benefit; real training instability found.** Pixel metrics (`033`) are a wash — differences between `unet`/`unet_dilated` and `unet_v2`/`unet_v2_dilated` are small and inconsistent in sign across loss/mae/psnr/ssim. AUROC/coherence (`040`, GT; `050`, full test set) lean slightly *negative* for both dilated variants on the larger, less noisy `050` sample. More informative than the final metrics: the **training dynamics**, read from each run's `history.json`. `unet_v2`/`unet_restormer` (non-dilated) were both still near their best val_loss at the epoch-100 cap (best at epoch 98/100 for both). Both dilated variants peaked early and then **degraded** and never recovered — `unet_dilated` peaked at epoch 55 (val_loss 0.237) then rose to 0.260 by epoch 75, when the new `EARLY_STOPPING_MIN_DELTA=5e-4` (see below) correctly stopped it; `unet_v2_dilated` peaked at epoch 3 (val_loss 0.248), spiked to 0.313 by epoch 4, and never got back below its epoch-3 value in the following 19 epochs before stopping. This is optimization instability, not just "faster convergence" — the wide multi-branch concat-then-1x1-project block likely creates a harder loss landscape than the plain bottleneck, or interacts poorly with weight decay/LR tuned for the non-dilated recipe. `ModelCheckpoint(save_best_only=True)` means the evaluated checkpoints are each run's actual best epoch, so this doesn't invalidate the `033` comparison above — it explains *why* the dilated variants have nothing to show for it despite reaching a good point early.
  - **Closed, not investigated further, by explicit user decision** (2026-08-27): the exploratory round's honest result is negative for this specific design (bottleneck-only dilation). No LR/recipe tuning attempted to fix the instability — would be a new experiment, not a continuation of #7's original scope.
  - **Re-measured against the two earlier generations** (2026-08-27, user request, no re-opening of the design): `C2_v1_v2.ipynb` generalised from a two-column (`v1` vs. `v2`) to a three-column comparison — `v1` / `v2` / `v2 dilated` — for the two families that have all three checkpoints (`unet` → `unet_dilated`, `unet_v2` → `unet_v2_dilated`). Its tables print the two *consecutive* deltas `Δ v2-v1` and `Δ v2 dilated-v2`, so the fixing rounds' effect and the bottleneck's effect on top of them are separated rather than summed. Complements `033`/`050` (which compare the dilated variants only against their current-generation baselines) by putting the pre-fix checkpoint in the same table.
  - Also this session: `settings.EARLY_STOPPING_MIN_DELTA` raised `0.0` → `5e-4` (`scripts/config.py`) after `unet_v2`'s in-progress run showed sub-1e-3 validation noise kept resetting the patience counter and running to the full `EPOCHS` cap well past real convergence. Saved real compute on the two dilated runs above (25 and 77 epochs respectively) without affecting which checkpoint gets saved (`ModelCheckpoint` still always saves the true best).
- **Beta sweep (extends #10) — colleague-initiated, code implemented, not yet run.** Outside the Round 0-4 structure: a direct follow-up question on #10's `laplace_nll_loss` β, not a new finding. A colleague pushed 6 concurrent commits to `origin/project-fixing` (merged this session, `41b6568`, zero file overlap so no conflicts): `024_training_beta_sweep.ipynb` (trains `unet_nll` at β ∈ {0.0, 0.25, 0.75}, holding everything else at the Round 1/2 recipe — β=0.5 is the existing `021` checkpoint, not retrained), a `--nll-beta` flag on `scripts/train_single.py` to support it, and `C0_v1_v2.ipynb`/`C1_v1_v2_beta.ipynb`/`C2_v1_v2.ipynb` (pre-fix-vs-current-generation comparisons, unrelated to the beta question but sharing its evaluation machinery — `C1`'s §8 calibration block is what `024` points to for judging the beta sweep). Two things done this session to make the colleague's work usable here:
  - **`models/v1/` path mismatch fixed, filesystem-only**: `C0`/`C1`/`C2` expect a flat pre-fix backup at `models/v1/<arch>/` (the colleague's own naming); this branch's equivalent is `models/pf1/`, nested by family (`deterministic/`, `nll_gaussian/`, `nll_beta/`). Since `models/` is entirely gitignored this could never have surfaced via the repo. Fixed with symlinks (`models/v1/<arch>` → `models/pf1/deterministic/<arch>`, etc.) matching the exact shape those notebooks expect — nothing to commit, verified every path resolves via `settings.MODELS_DIR`.
  - **New `C3_beta_sweep.ipynb`** (committed, `419d352`): adapts `C0`/`C1`'s fidelity/detection/coherence/calibration machinery from a 2-way (pre-fix vs. current) comparison to a 4-way β comparison on `unet_nll`. Unlike `C1` (Gaussian `v1` vs. Laplace `v2` — two different densities, `nll` marked not comparable), every β here is the same Laplace distribution, so `evaluate_calibration`'s `nll` field is directly comparable across columns (confirmed by reading `scripts/calibration.py`: it's the plain unweighted Laplace NLL, not the β-reweighted training loss). §9's verdict tallies each β against the β=0.5 reference.
  - **Not yet run**: `024`'s three checkpoints (`models/beta_sweep/beta_{0.00,0.25,0.75}/unet_nll/`) don't exist on disk yet, so `C3` has nothing to compare until `024` runs first.
- **Round 4 — grouped k-fold cross-validation, implemented, trained, evaluated. Result: the single split was adequate — #4 resolved.** Scope narrowed (from `C4`/`C5`/`final-comments.md`) to **one model**, `attention_unet_nll`, **k=3**, **β=0.5** — a variance estimate, not a model search. Infra: `scripts/kfold.py` (`grouped_kfold_splits`/`fold_split`/`fold_artwork_groups` — real artworks partitioned by ID into 3 groups, mockups always in train, GT paintings external to every fold, deterministic in `KFOLD_SEED=42`); `scripts/train_single.py --fold i --kfold-k K`; `config.KFOLD_K=3`/`KFOLD_SEED=42`; `notebooks/060_kfold_training.ipynb` (skip-if-exists loop, one subprocess/fold) + `061_kfold_evaluation.ipynb`. 259/259 unit tests (`test_kfold.py`). `models/kfold/` filesystem-only.
  - **Trained (2026-08-27)**: all 3 folds, no NaN/instability. Early-stopped epoch 22/38/24, best `val_loss` −0.3844/−0.4451/−0.4424. Fold 0 (largest held-out group) is ~0.06 harder than folds 1–2.
  - **Evaluated (`061` §6)**: held-out pixel spread `val_loss` −0.4240 ± 0.0280, MAE 0.103 ± 0.013. Detection on GT01–03, mean AUROC: `structural delta` 0.678 ± 0.008 per fold / 0.700 ensemble; `structural z` 0.699 ± 0.008 per fold / 0.719 ensemble. Fold-to-fold std (~0.008 AUROC) is far below any ranking gap — `structural z` ≥ `structural delta` on every fold; per-GT pattern (GT01 strong, GT03 weak) unchanged from `C5`; the 3-fold ensemble (mean of fold `μ`s) beats every single fold on both signals.
  - **Verdict**: cross-validation **confirms** the single-split estimates (`031`/`C4`: `attention_unet_nll` `structural delta` ≈ 0.70, `structural z` ≈ 0.71) rather than deflating them. #4 resolved as "single split was adequate"; no second model needed (fold variance not alarming). Headline for the write-up: **`structural z` AUROC 0.70 ± 0.01 (per fold), 0.72 (3-fold ensemble)** for `attention_unet_nll`. Written up in `evaluation.md` §8.
- **Residual output head + `combined_loss` alpha sweep — user-initiated, code implemented, not yet run.** Outside the Round 0-4 structure and not a finding: a user-proposed architecture variant (2026-08-27), implemented as a new checkpoint rather than a change to any existing one. `scripts/unet_residual.py` (`unet_residual` in `_BUILDERS`) reuses `scripts.unet._conv_block` — same backbone object, not a copy — and replaces only the head: a `tanh` `1x1` conv predicts a *signed* residual, added to `mean(R, G, B)` and clipped to `[0, 1]`, so the reference grey level costs no parameters and the network learns only where IR departs from it (global residual learning: VDSR, Kim et al. 2016; DnCNN, Zhang et al. 2017). Verified identical parameter count to `unet` (31,037,633 both) — any difference is attributable to the output parameterization, not to capacity.
  - **The clip is a straight-through estimator**, not `tf.clip_by_value` (`ClipToUnitStraightThrough` in `scripts/residual_head.py`): `x + stop_gradient(clip(x) - x)` is exact forward and transparent backward. A plain clip has zero gradient outside the range, so with `gray` already spanning all of `[0, 1]` before a `tanh` residual in `[-1, 1]` is added, any overshooting pixel would be permanently dead. Both head layers (`RGBToGray`, `ClipToUnitStraightThrough`) are `register_keras_serializable(package="deep_layers")` layers for the same reason `scripts/nll_layers.py` exists — raw `tf` ops and `Lambda` layers do not survive `load_model`. Round-trip test included.
  - **No new loss was needed.** The user's requested `alpha * L1 + (1 - alpha) * MS-SSIM` at `alpha ∈ {0.16, 0.5, 0.84}` is exactly the existing `losses.combined_loss(alpha)` (its fidelity term is Charbonnier, i.e. L1 smoothed at `eps=1e-3`, #9), already parameterized as `settings.LOSS_ALPHA` and as `compile_model(loss_alpha=...)`. Only the CLI was missing: `scripts/train_single.py --loss-alpha`, mirroring `--nll-beta`, so each run's alpha is recorded in the command that launched it (it does not survive into the checkpoint). Also corrected in passing: the proposal's 3-scale `power_factors` workaround is unnecessary (`CROP_SIZE=256` clears the ~176px minimum for the default 5 scales) and would silently rescale the term, since a truncated 3-factor list sums to 0.63 rather than 1.0.
  - **`025_training_residual_head.ipynb`** trains the three alphas, one subprocess each (§7's fresh-process rule). `alpha = settings.LOSS_ALPHA` lands in the canonical `models/deterministic/unet_residual/` so `030`-`050` resolve it by name; the other two in `models/alpha_sweep/alpha_<value>/unet_residual/`. The `alpha = 0.16` run against `020`'s existing `unet` checkpoint is the only *directly* comparable pair (same loss function, head is the only difference); across alphas the loss is a different function and its value is not a ranking, same caveat as `024` §4.
  - **Known risk, stated up front**: `mean(R, G, B)` is a weak IR prior — infrared reflectance is not visible luminance, and pigments with equal grey values can be IR-opaque or IR-transparent. If the required residual is large and structured the free-identity advantage disappears. Empirical question about these paintings; that is what the run is for.
  - **Trained (2026-08-28)**, all three alphas: `models/deterministic/unet_residual/` (α=0.16, ran to the 100-epoch cap, best `val_loss` 0.3144 / `val_mae` 0.1819), `models/alpha_sweep/alpha_0.50/` (62 epochs, 0.2394 / 0.1501), `models/alpha_sweep/alpha_0.84/` (68 epochs, 0.1774 / 0.1494). Only the α=0.16 run is loss-comparable to `unet` (0.2373 / 0.1679, 25 epochs) — on that matched pair the residual head is **worse on both**.
  - **`034_evaluation_residual_head.ipynb`** scores all three against `unet` on `data/test/`: fidelity, detection AUROC/AP, stroke coherence, plus two diagnostics that exist only for this head — `resid_l1 = mean |mu - gray|` (collapse detector) and `clip_frac` (clip occupancy) — and a **`gray` control column**: `mean(R, G, B)` scored as if it were a model, the floor any 31M-parameter run has to clear. §11 plots one on-demand image without redoing the whole pass, since §4 discards each prediction after scoring it.
  - **Evaluated (2026-08-28, `034`, all 10 `data/test/` images). The head buys pixel fidelity and gives up exactly the structure the project needs.** Three separate results:
    - **The α=0.16 run collapsed to the identity.** `resid_l1 = 0.0027` against `unet`'s 0.0887 — the `tanh` head learned essentially nothing, and the model is an expensive `mean(R, G, B)`: `mae` 0.1397, *worse* than the 0.1381 of the parameter-free grey control it degenerated into. 31M parameters and a 100-epoch run producing less than one `numpy` call. Plausible mechanism, not separately tested: at α=0.16 the loss is 84% MS-SSIM and the free grey path already carries all of the RGB's structure, so there is little gradient pressure to build a real residual. **The project's default α is precisely the one that kills this head** — which is why the sweep, not a single run, was the right call.
    - **α=0.50/0.84 use the residual and win on fidelity**: `resid_l1` 0.1030/0.1102, `mae` 0.0982/0.0980 vs. `unet`'s 0.1265 (−22%), `psnr` +1.6 dB. They **lose on `ssim`** (0.5085 vs. 0.6071). `clip_frac` ≤ 0.0004 everywhere, so the straight-through clip is never load-bearing and the `tanh` range is not being fought.
    - **On detection — the axis that actually matters — every variant is worse than `unet` and indistinguishable from the grey control.** `structural delta` AUROC: `gray` 0.6702, `unet` 0.7073, residual 0.6707/0.6700/0.6700. Stroke coherence, same pattern: `gray` 0.277, `unet` 0.357, residual 0.278/0.277/0.277. So even at α=0.84, where the residual *is* in use, it improves absolute level agreement without adding local structure — consistent with an 84%-Charbonnier objective, which optimizes level, not structure. The underdrawing signal is a structural signal, so a head that trades SSIM/coherence for MAE trades away the thing being measured.
  - **Verdict**: negative for underdrawing detection, and not a marginal call — the residual variants' `structural delta` matches a channel mean to three decimal places. A fidelity-only use of RGB→IR translation would read this differently (α=0.84 is the best `mae`/`psnr` model in the deterministic family), so the result is recorded rather than declared a dead end. No further α tuning attempted; that would be a new experiment.

---

## 1. Findings, priority, and change type

Ranked most to least severe, as in `theory-links.md` §8.4-8.10.

| # | Finding | Priority | Change type | Retrain scope |
|---|---|---|---|---|
| 1 | `BatchNorm` with `BATCH_SIZE=8` (18 BatchNorm layers in `unet`, batch size ~10-30x below the 50-256 range two lectures cite as typical) | Highest — best-evidenced (two independent lectures flag the same parameter) | Architectural — replace `BatchNormalization` with `GroupNormalization` | All BatchNorm-using architectures |
| 2 | No weight decay anywhere (`kernel_regularizer`, `regularizers`, `Adam(weight_decay=...)` all absent) | High | Hyperparameter — add `kernel_regularizer` or `Adam(weight_decay=...)` | All architectures |
| 3 | All 23 conv layers use `GlorotUniform` (Xavier) with ReLU activations — the literature's own stated bad pairing | Medium | Hyperparameter — `kernel_initializer="he_normal"` | All architectures (mitigated by BatchNorm/GroupNorm, so lower urgency than #1/#2 despite being "free") |
| 4 | No cross-validation — single fixed split, point-estimate metrics only | Medium — **resolved** (see §0): grouped k-fold (`attention_unet_nll`, k=3) run; fold-to-fold std ≈ 0.008 AUROC confirms the single-split metrics were trustworthy | Evaluation methodology — grouped k-fold on top of the existing artwork-grouped split | K trainings of `attention_unet_nll` only (scope narrowed from all models) |
| 5 | `resunet.py`'s residual shortcut is an unconditional learned projection (`Conv2D 1×1` + BatchNorm on every block), not the pure identity path the "uninterrupted additive path" argument (L06 slides 77-78) requires | Medium — **resolved as not applicable** (see §0): every block always changes channel count, so a conditional identity path would be dead code | Architectural — identity shortcut where channels already match | `resunet`, `resunet_nll` — kept as unconditional projection (GroupNorm/He init still applied) |
| 6 | Two-phase EfficientNet fine-tuning unused (`freeze_encoder=False` exists, never set) | Lower | Training procedure — unfreeze encoder in a second training phase | `efficientnet_unet`, `efficientnet_unet_nll` only |
| 7 | Dilated convolution never tried — the standard way to widen receptive field without downsampling, which is this project's core architectural tension | Lower — **implemented as a dilated bottleneck and closed with a negative result** (see §0): no measurable metric benefit, and real training instability vs. the non-dilated baseline | Architectural — new variant, `dilation_rate` on selected conv layers | New variants only, not a fix to an existing model |
| 8 | Documentation-only gaps (heteroscedastic-head universality/manifold argument, annotation-budget rationale, augmentation label-preservation principle — none written up) | Lowest | Documentation only | None |
| 9 | Deterministic-family loss is `ℓ1`/`ℓ2`-dominant, and `efficientnet_unet` has no perceptual term at all (Zhao et al. 2016 — see §5). `combined_loss`'s `LOSS_ALPHA=0.7` weights MAE over SSIM in the opposite proportion to the paper's best-performing config; `combined_loss_advanced` (MAE + Laplacian pyramid + FFT) has no SSIM/MS-SSIM term | High — direct, well-evidenced fix, not merely an omission | Hyperparameter/loss-formula — unify all 6 deterministic architectures under one loss, `0.16·Charbonnier + 0.84·(1−MS-SSIM)` (colleague proposal, matches the paper's `Mix`) | All 6 deterministic architectures (every one that trains against `combined_loss`/`combined_loss_advanced`) |
| 10 | NLL-family loss is Gaussian (`ℓ2`-weighted uncertainty); no Laplace (`ℓ1`-weighted) alternative has been tried, despite the same L1-over-L2 evidence applying to the likelihood term as to the deterministic fidelity term | High — same evidence base as #9, applied to the likelihood term | Loss-formula — replace `gaussian_nll_loss`/`beta_gaussian_nll_loss` with a single Laplace NLL, `stop_grad(b)^β · (\|y−μ\|/b + log b)`, β=0.5 (colleague proposal); collapses the `nll_gaussian`/`nll_beta` split into one loss per architecture | All 4 NLL architectures — collapses 8 NLL checkpoints (4 architectures × {gaussian, beta}) into 4 (one per architecture) |

---

## 2. Retrain-minimizing plan

The naive approach — one retrain pass per finding — would mean up to 8 full passes over the checkpoint tree. Findings #1-#3, #5, #9, and #10 are all compatible changes to the same training run (none undoes another's effect), so they are bundled into shared retrain rounds instead. #4, #6, #7 are kept as separate rounds because they either multiply cost (#4), are scoped to a disjoint set of models (#6), or are exploratory additions rather than fixes (#7).

**Note on #9/#10 (added after the colleague's loss-function review, following the Round 0 pilot)**: both are retrain-requiring changes — a different loss changes what the optimizer minimizes, so every checkpoint trained under the old loss must be retrained regardless of whether its architecture changes. Since #9/#10 touch the same models already being retrained for #1/#2/#3/#5 in Round 1, and the EfficientNet models already being retrained in Round 2, they are folded into whichever round already retrains each model — no new round, per the decision to minimize retrain cycles.

### Round 0 — Pilot validation (small, before committing to a full pass)

- **Models**: `unet`, `unet_v2` only.
- **Change**: #1 (`BatchNormalization` → `GroupNormalization`) in isolation.
- **Why first and alone**: #1 is the highest-priority finding but also the riskiest — it is the only one of the bundle that changes how activations are normalized at every layer. Validating it cheaply on two representative deterministic architectures before committing #1 across the whole tree avoids discovering a regression only after 12 models have been retrained.

### Round 1 — Combined "recipe v2" retrain

- **Models**: `unet`, `unet_v2`, `resunet`, `attention_unet`, `unet_restormer` (5 deterministic architectures) + `unet_nll`, `resunet_nll`, `attention_unet_nll` (3 NLL architectures — `unet_v2`/`unet_restormer` have no NLL counterpart in the current tree). 8 checkpoints total (down from the 11 that these architectures represent in the current 14-checkpoint tree, since #10 collapses each NLL architecture's `nll_gaussian`/`nll_beta` pair into a single checkpoint). EfficientNet-based models are handled separately in Round 2.
- **Changes bundled in one pass**:
  - #1 `BatchNormalization` → `GroupNormalization` (`num_groups=32`, uniform across these architectures — see §3 below for why)
  - #2 weight decay (`Adam(weight_decay=...)` or `kernel_regularizer`)
  - #3 `kernel_initializer="he_normal"`
  - #5 identity shortcut in `resunet`/`resunet_nll` (bundled here since those two models are already being retrained in this pass)
  - #9 unified deterministic loss (`0.16·Charbonnier + 0.84·(1−MS-SSIM)`) for `unet`, `unet_v2`, `resunet`, `attention_unet`, `unet_restormer`
  - #10 unified NLL loss (Laplace-beta, β=0.5) for `unet_nll`, `resunet_nll`, `attention_unet_nll` — replaces both `gaussian_nll_loss` and `beta_gaussian_nll_loss` for these three architectures
  - Retuned, parametric training callbacks (`val_loss` monitored uniformly across checkpoint/early-stop/LR-reduce, new patience/factor/cooldown/min_delta/min_lr values sourced from `settings.*` — see §5)
- **Why bundled**: none of these changes interacts adversarially with another — weight decay, initializer, and loss-formula changes only affect the optimization objective/trajectory, not the forward-pass structure that #1 changes (except for the NLL head's second output channel, see §5). Splitting them into separate full retrain passes would multiply the compute cost for no gain in signal, since the plan does not require isolating each one's individual marginal contribution before shipping.
- **`unet_restormer` exception**: already uses `LayerNormalization` inside `RestormerBlock` (a Transformer-derived block, where LayerNorm is the established choice — see §3). Left unmodified by #1; #2/#3/#9 still apply to it in this round (it has no NLL counterpart, so #10 does not apply).

### Round 2 — EfficientNet, handled separately

- **Models**: `efficientnet_unet`, `efficientnet_unet_nll` (now a single checkpoint, per #10's collapse), plus a new `efficientnet_unet_ft` variant (see below). 3 checkpoints total.
- **Why separate from Round 1**: `efficientnet_unet(_nll)` uses a pretrained EfficientNetB0 encoder. Applying #1 to the encoder's BatchNorm layers would destroy the pretrained batch statistics, defeating the purpose of transfer learning — so #1 is scoped to the custom decoder only (`decoder_filters = [256, 128, 64, 32, 16]`, see §3 for the group-count implication).
- **Changes**: #1 (decoder only), #2, #3 applied to the decoder; #6 (two-phase fine-tuning) applied as a second training phase; #9 (unified deterministic loss, replacing `combined_loss_advanced` — this also removes the Laplacian-pyramid/FFT terms, see §5) for `efficientnet_unet`; #10 (unified NLL loss) for `efficientnet_unet_nll`.
- **On saving the two-phase fine-tuning as a separate checkpoint** (user question, resolved here): yes — save the frozen-encoder result as `efficientnet_unet` (current behavior, updated with the recipe-v2 changes) and the unfrozen second-phase result as a distinct new checkpoint, e.g. `efficientnet_unet_ft`, rather than overwriting one with the other. This lets `052_model_comparison.ipynb` compare frozen vs. fine-tuned directly instead of losing the frozen baseline.
- **§7's three NaN fixes need to reach this round too** (flagged by the user; done — see §0): §7's `_ReLUFix` is applied to `efficientnet_unet.py`'s custom decoder blocks (the pretrained encoder itself is out of reach — still Keras Applications' own ReLU/Swish, see §7's caveat), and §7.2's `Adam(clipvalue=...)` covers it for free (`compile_model` applies it unconditionally to every architecture). `022_training_efficientnet.ipynb` now trains through §7.1's `scripts/train_single.py` subprocess-per-architecture pattern like `020`/`021`/`023`, including the two-phase fine-tuning orchestration (`--kwargs`/`--init-from`/`--lr`) and a `--nll --loss-name laplace_nll` path.

### Round 3 — Dilated convolution (exploratory)

- **Models**: new variants of `unet`, `unet_v2` (as requested), saved under distinct names (e.g. `unet_v2_dilated`) rather than overwriting the Round 1 checkpoints.
- **Why its own round**: #7 is a new architectural direction, not a fix to an existing design choice — it needs to be evaluated on its own against the Round 1 baseline, not conflated with the BatchNorm/weight-decay/init changes.

### Round 4 — Cross-validation (#4), last, may not happen

**Decided and tooled (2026-08-27), not yet run.** Scope narrowed to **one model,
`attention_unet_nll`, `k=3`, β=0.5 fixed** — from `C4`/`C5`/`final-comments.md`:
`attention_unet(_nll)` is top-2 on every detection cut (`struct` AUROC 0.70,
`sz learned` AUROC 0.71, coherence 0.29), the deliverable criterion is
detection/coherence (not pixel fidelity), and the NLL head covers both signals
(`structural delta` from its μ, `structural z`). A second model is added only if
fold variance turns out alarming. Infrastructure: `scripts/kfold.py`
(`grouped_kfold_splits` — real artworks partitioned by ID into k groups, mockups
always in train, no test split), `scripts/train_single.py --fold i --kfold-k K`,
`config.KFOLD_K`/`KFOLD_SEED`, notebooks `060_kfold_training.ipynb` (skip-if-exists,
one resumable subprocess per fold — run across several short sessions) and
`061_kfold_evaluation.ipynb` (per-fold held-out pixel metrics + GT detection AUROC
per fold-model and as a 3-fold ensemble; tolerates partial runs). 259/259 unit tests
(new `test_kfold.py`). Real-artwork groups: 25 → ~8-9 per fold, 214-333 val pairs.

- **Models** (original note, superseded by the decision above): limited to the 2-3 best-performing architectures resulting from Rounds 1-3 (not the full tree), to bound the combinatorial cost — k-fold multiplies training cost by k per model included. Which 2-3 is itself unresolved: pixel metrics (`030`/`033`) and detection/coherence (`040`/`050`) disagree — `resunet`/`resunet_nll` lead the former, `unet`/`attention_unet`/`unet_v2`/`unet_nll` lead the latter (see the Done section's Round 1-3 results). Since the project's deliverable is the detected residual, not IR fidelity for its own sake, detection/coherence is the more defensible criterion if only one has to be picked — but this needs to be decided and stated explicitly before the round starts, not left implicit.
- **Why last**: k-fold validation should run against the final recipe, not be repeated every time an upstream change (Round 1-3) is introduced. Running it first would mean re-running the whole k-fold sweep after every subsequent architecture change.
- **Fold count and time estimate** (this session, 2026-08-27): `k=5` is the textbook default; `k=3` is the pragmatic floor still worth calling "cross-validation" (a genuine held-out variance estimate across 3 independent splits, vs. today's single fixed split) rather than `k=5`'s finer but ~1.7x-costlier estimate. With ~30 distinct artwork/mockup groups in `data/`, `k=5` gives ~6 groups/fold and `k=3` gives ~10 — both are plausible group counts for the grouped split this multiplies, so the choice is really about compute budget, not statistical validity. Time per fold-run, measured this session (`unet_v2`'s real training log, 108 train batches, ~195 s/epoch on this hardware): **~5.4 hours** for a 100-epoch run (both `unet_v2` and `unet_restormer` were still improving at the 100-epoch cap this session, so 100 is the realistic planning number for a healthy architecture, not a worst case). Total (sequential — this project trains one model per subprocess, no parallelism):

  | model set | `k=3` | `k=5` |
  |---|---|---|
  | **2 — pixel-metric pair**: `resunet` + `resunet_nll` | 6 runs, **~1.35 days** | 10 runs, **~2.3 days** |
  | **2 — detection-metric pair**: `unet` + `unet_nll` | 6 runs, **~1.35 days** | 10 runs, **~2.3 days** |
  | **3** (any mix of the above plus a third, e.g. `attention_unet`) | 9 runs, **~2 days** | 15 runs, **~3.4 days** |
  | **4 — both pairs combined**: `resunet`, `resunet_nll`, `unet`, `unet_nll` | 12 runs, **~2.7 days** | 20 runs, **~4.5 days** |

  (Using the 100-epoch/run, ~195 s/epoch estimate. The two 2-model rows are the same size by coincidence — one deterministic + one NLL architecture either way — not because the two selection criteria agree on which architectures those are.) Given these numbers next to the time already spent on Rounds 0-3, **the 2-model, `k=3` row (~1.35 days) is the realistic floor if this round happens at all**; scaling up to 3-4 models and/or `k=5` should only be considered if there is clearly enough time left after everything else in the To-do list.

### Outside the rounds — no retrain

- **#8** (documentation gaps): can be written up at any time, in parallel with the rounds above; blocks nothing and is blocked by nothing.

---

## 3. GroupNorm vs. LayerNorm, and group-count implications

**Why replace BatchNorm rather than increase batch size**: images in this project run up to ~3700×2800px (whole-image inference via `pad_to_multiple` + `model.predict()`, no tiling); a batch size in the 50-256 range cited as typical would likely exceed available GPU memory at that resolution, and raising batch size also requires re-tuning the learning rate (conventionally scaled with batch size), turning a one-line fix into a new hyperparameter search — while still requiring a full retrain either way. Replacing the normalization layer fixes the root cause (BatchNorm's per-batch statistics are noisy at N=8) independently of batch size, at the same retrain cost.

**GroupNorm over LayerNorm for the plain conv architectures**: GroupNorm (Wu & He, 2018) was introduced specifically to address BatchNorm's small-batch instability in vision models, normalizing within channel groups rather than across a full batch. LayerNorm normalizes across all channels at once and is the established choice for Transformer/RNN-style blocks where a "token" has no strong per-channel spatial structure — which is why it is appropriate inside `RestormerBlock` (attention-based) but not the natural fit for the plain convolutional blocks in `unet`/`unet_v2`/`resunet`/`attention_unet`.

**Channel counts verified in `scripts/`**:

| Architecture | Encoder/decoder channels | Bottleneck |
|---|---|---|
| `unet`, `unet_v2`, `resunet` (+ `_nll`) | `[64, 128, 256, 512]` | `1024` |
| `attention_unet` (+ `_nll`) | `[64, 128, 256, 512]` (attention-gate intermediate channels `f // 2` = `[32, 64, 128, 256]`, but those 1×1 convs carry no BatchNorm in the current code, so #1 does not touch them) | `1024` |
| `efficientnet_unet` (+ `_nll`) | decoder: `[256, 128, 64, 32, 16]` | reduction from `1280` (pretrained encoder) → `256` |

All values are powers of two, but they are not uniform across every layer — the EfficientNet decoder goes down to 16 channels. Implication: `num_groups=32` is safe as a uniform choice for `unet`/`unet_v2`/`resunet`/`attention_unet` (divides every layer from 64 to 1024), but the EfficientNet decoder needs a smaller or adaptive `num_groups` (e.g. capped at the layer's channel count) for its final 16-channel block, since 32 does not divide 16. This is an implementation detail for Round 1/2, not a blocker for this plan.

---

## 4. Loss-function findings (#9, #10)

Surfaced from a direct question about whether the current losses are correct for the RGB→IR task, cross-checked against Zhao, Gallo, Frosio & Kautz, *"Loss Functions for Image Restoration with Neural Networks"* (2016) — a paper specifically about the loss layer's impact on image-restoration quality — and then refined by a colleague's proposed unification.

**What the current losses already get right**: neither `combined_loss` nor `combined_loss_advanced` ever uses plain `ℓ2`, which the paper identifies as the single biggest source of avoidable quality loss (splotchy artifacts in flat regions, poor correlation with perceived quality) — both already lean on `ℓ1`-family + a structural/frequency term, the right general direction.

**#9 — Deterministic family**: the paper's best-performing loss (`Mix`) is `0.84·MS-SSIM + 0.16·ℓ1`, empirically outperforming `ℓ2`, `ℓ1`, `SSIM` alone, and `MS-SSIM` alone on every quality metric tested (Table I). Two concrete gaps against this project's current losses:

- `combined_loss`'s `LOSS_ALPHA=0.7` weights the fidelity term (MAE) at 70% and the perceptual term (SSIM) at only 30% — the inverse emphasis of the paper's finding.
- `combined_loss` uses single-scale `tf.image.ssim`, not multi-scale. The paper shows single-scale SSIM is a forced trade-off (small `σ_G` fixes edges but reintroduces flat-region artifacts; large `σ_G` does the opposite) — precisely the reason MS-SSIM exists. `tf.image.ssim_multiscale` is a direct drop-in.
- `combined_loss_advanced` (`efficientnet_unet` only) has no perceptual term at all — just MAE + Laplacian pyramid + FFT magnitude, none of which model HVS luminance/contrast/structure sensitivity the way SSIM-family metrics do.

The colleague's proposed unification, `loss = 0.16·Charbonnier(y, ŷ) + 0.84·(1 − MS-SSIM(y, ŷ))` for **all 6** deterministic architectures, resolves all three: matches the paper's weighting exactly, upgrades to multi-scale, and gives `efficientnet_unet` a perceptual term for the first time — at the cost of dropping its Laplacian-pyramid/FFT terms, which is a real design trade-off (those terms were specifically motivated for that architecture's richer pretrained features) worth confirming empirically rather than assuming as a pure improvement. Charbonnier (`sqrt((y−ŷ)² + ε²) − ε`) is a smooth `ℓ1` approximation standard in image-restoration literature, preferred over raw `ℓ1` for its defined gradient at zero error.

**#10 — NLL family**: the paper's finding — `ℓ1`-family losses beat `ℓ2`-family — applies just as much to the likelihood term of the heteroscedastic models as to the deterministic fidelity term. The current `gaussian_nll_loss` (`0.5·exp(-log_var)·(y−μ)² + 0.5·log_var`) is exactly the NLL of a Gaussian, i.e. `ℓ2`-weighted-by-uncertainty. The colleague's proposal, `stop_grad(b)^β · (|y−μ|/b + log b)` with β=0.5, is the NLL of a **Laplace** distribution (scale `b`) instead — the `ℓ1`-weighted-by-uncertainty analogue, with the same Seitzer et al. (2022) beta-reweighting trick applied to the Laplace scale rather than the Gaussian variance (a motivated generalization, not the literally published formula — worth documenting as such, the way `code-review.md` §7.6 already documents the existing beta-NLL choice).

Two implementation notes for whoever picks this up:
- The NLL heads' second output channel is currently named/interpreted as `log_var` (`ClipLogVar`, `NLL_LOG_VAR_MIN/MAX`). Under Laplace it becomes `log_b` (log-scale, not log-variance) — same layer, same clip mechanism, but the sensible numeric range for `b` is not necessarily the same as for `log_var`, so the clip bounds should be re-checked rather than assumed to transfer.
- Per the decision recorded when this finding was added: the Laplace-beta loss **replaces** both `gaussian_nll_loss` and `beta_gaussian_nll_loss` (rather than being added as a third variant), collapsing the `models/nll_gaussian/`+`models/nll_beta/` split into one NLL checkpoint per architecture. This deliberately closes the gaussian-vs-beta comparison axis that `.claude/handoff/HANDOFF.md` records as having a "mixed empirical record" — a considered trade-off (simpler tree, one fewer axis to maintain), not an oversight.

---

## 5. Callback tuning (Round 1)

Surfaced while discussing #9/#10: since the training objective changes shape in Round 1 (`combined_loss`'s balance shifts from MAE-dominant to 84%-MS-SSIM-dominant), the callbacks that decide when to checkpoint/stop/reduce LR should be reconsidered alongside it, not left as-is. A colleague proposal split the three callbacks across different monitored metrics (`val_mae` for checkpointing, `val_ssim` for early-stopping/LR); discussed and rejected in favor of monitoring **`val_loss` uniformly across all three** — since `val_loss` already *is* the actual training objective in whatever proportion #9 lands on, splitting onto different metrics risks saving a checkpoint that has no relationship to the plateau that determined how long training ran, and `val_ssim` alone (single-scale) doesn't even match what the loss now optimizes (MS-SSIM).

**Implemented** (see §0). `scripts/config.py`/`scripts/trainer.py`/`scripts/trainer_nll.py`:

- New fields added to `scripts/config.py` (`Settings`), with the values discussed here as defaults:
  - `EARLY_STOPPING_PATIENCE: int = 20` (was hardcoded `15` in `trainer.get_callbacks`)
  - `EARLY_STOPPING_MIN_DELTA: float = 0.0`
  - `EARLY_STOPPING_RESTORE_BEST_WEIGHTS: bool = False` (was hardcoded `True`; safe because every notebook reloads the best checkpoint from disk rather than reusing the in-memory `model` post-`fit()` — see caveat below)
  - `REDUCE_LR_FACTOR: float = 0.25` (was hardcoded `0.5`)
  - `REDUCE_LR_PATIENCE: int = 6` (was hardcoded `7`)
  - `REDUCE_LR_COOLDOWN: int = 2` (new — `get_callbacks` currently passes no `cooldown`, so it defaults to `0`; prevents rapid repeated LR drops right after a reduction)
  - `REDUCE_LR_MIN_DELTA: float = 1e-4` (new — currently unset/`0`)
  - `REDUCE_LR_MIN_LR: float = 1e-6` (was hardcoded `1e-7`)
- All three callbacks' `monitor` set to `"val_loss"` (`mode="min"`, the Keras default for a loss — no `mode=` override needed, unlike the rejected `val_ssim`/`val_mae` proposal which needed explicit `mode="max"`/`"min"`).
- `trainer.get_callbacks` now accepts these as parameters defaulting to the new `settings.*` fields (same pattern already used by `compile_model(lr: float = settings.LEARNING_RATE, ...)`), rather than hardcoding the literals inline as before. `trainer_nll` reuses the same `get_callbacks` (it never had its own).
- **Caveat to carry forward**: `restore_best_weights=False` only stays safe as long as no notebook evaluates the in-memory `model` object immediately after `fit()` instead of reloading `best_model.keras` from disk. True today (`020_training.ipynb` and this plan's future notebooks all reload via `trainer.load_model`); worth a one-line check if that convention ever changes.

---

## 7. tensorflow-metal ReLU NaN bug and workaround (Round 1 retraining)

Surfaced during the first real retraining attempt under the Round 1 recipe (`020_training.ipynb`): `unet` trained cleanly for several epochs before all metrics went `NaN` simultaneously (epoch ~5-6); `resunet` and `attention_unet` went `NaN` immediately, within epoch 1. Not a loss-function or hyperparameter problem — root-caused to a GPU backend bug.

**Diagnosis** (all steps reproduced directly on this project's hardware, not inferred):

1. Manually replicating `resunet`'s training loop with `GradientTape` in eager mode over a full real epoch (108 batches): no `NaN`, healthy loss curve.
2. Same model, same data, same optimizer, via `model.fit(..., run_eagerly=True)` (still graph-free): clean epoch, loss 0.36 → val_loss 0.29 — matching the eager result.
3. Same model via `model.fit()` in its default graph-compiled mode (the notebook's actual code path): `NaN` within epoch 1, every time.
4. Isolating `GroupNormalization` alone (forward + backward, 200 synthetic training steps, including exactly-zero-variance inputs): never `NaN`, in either eager or graph mode — ruling out GroupNorm itself as the direct cause.
5. Searched for a known cause: **[tensorflow-metal ReLU activation fails to clip negative values on M4 Apple Silicon](https://developer.apple.com/forums/thread/818015)** (Apple Developer Forums, open, unresolved) — reported on the exact stack used here (Mac M4, `tensorflow-macos==2.16.2`, `tensorflow-metal==1.2.0`). Reproduced verbatim on this machine: a `Dense(activation="relu")` layer with all-negative weights/bias returns negative outputs (min `-11.0`) when run on the Metal GPU in graph mode, vs. the correct `0.0` on CPU. `tensorflow-metal` has not had a release since January 2025 (`1.2.0` is still the latest on PyPI as of writing) — no upstream fix available.
6. Mechanism: every `_conv_block`/`_residual_block` in every architecture ends in a `ReLU`. If the compiled Metal kernel intermittently leaks negative activations through it, they accumulate over network depth (8-9 blocks) and are amplified faster by `resunet`'s `Add()` residual shortcut and `attention_unet`'s `Add()`+`Multiply()` attention gate than by `unet`'s plain `Concatenate()` — explaining why `resunet`/`attention_unet` diverge at epoch 1 while `unet` takes ~5 epochs of accumulation to reach the same failure.

**Workaround implemented** (`scripts/norm_utils.py`): a `_ReLUFix` layer (`call` returns `tf.maximum(x, 0.0)`) plus a `relu()` helper, used in place of every `layers.ReLU()`/`activation="relu"` call across all 9 architecture files (`unet.py`, `unet_v2.py`, `unet_restormer.py`, `resunet.py`, `attention_unet.py`, `unet_nll.py`, `resunet_nll.py`, `attention_unet_nll.py`, `efficientnet_unet.py`'s decoder). Confirmed to resolve the bug directly: `resunet` retrained with the fix, in the same default graph-compiled mode on GPU that previously went `NaN` within epoch 1, completed 2 clean epochs (loss 0.36 → 0.30, val_loss 0.29 → 0.26) — matching the `run_eagerly=True` numbers exactly, at full GPU speed (no eager-mode slowdown).

A `Lambda(lambda t: tf.maximum(t, 0.0))` was tried first and rejected: mathematically identical, but Keras 3 refuses to deserialize a `Lambda` layer's Python function from a `.keras` checkpoint by default (arbitrary-code-execution guard), which broke 5 of this project's save/load round-trip unit tests. `_ReLUFix` is a proper registered `Layer` subclass instead (same `register_keras_serializable` pattern already used by `efficientnet_unet._ResizeToMatch`), which serializes cleanly — all 235 tests pass with it.

**Caveat**: `efficientnet_unet`'s pretrained EfficientNetB0 *encoder* still uses Keras Applications' internal ReLU/Swish implementation, unmodified — patching it would mean reimplementing the encoder. Not a blocker for Round 1 (this architecture is Round 2 territory), but worth remembering if Round 2 retraining shows the same NaN pattern.

**Status**: fix implemented and unit-tested. Turned out to be one of three independent bugs surfaced during Round 1 retraining — see §7.1 and §7.2 below for the other two, found while retesting this one.

### 7.1. GPU state accumulation across sequential `model.fit()` calls

After the ReLU fix (§7), rerunning `020_training.ipynb` (kernel restarted, fix active) showed a new pattern: whichever architecture trains **first** in the `for arch in ARCHS` loop always trains cleanly; every architecture after it increasingly risks going `NaN` from epoch 1 — reproduced with two different orderings (`unet` first / `resunet`,`unet` first), always same shape: first clean, rest `NaN`.

**Diagnosis**: `scripts/trainer.py`'s notebooks never called `tf.keras.backend.clear_session()` between architectures in the loop. Reproduced directly: `resunet` then `unet` trained back-to-back in one process with no `clear_session()` — `resunet` (first) clean, `unet` (second) `NaN` from epoch 1, on the exact same code that trains `unet` cleanly when it runs alone. Adding `clear_session()` between iterations fixed the second model but not consistently the third — `tensorflow-metal` does not appear to fully release GPU memory back to the OS on `clear_session()`, so state still accumulates, just more slowly.

**Fix implemented**: `scripts/train_single.py` (new) — trains exactly one architecture, invoked as a subprocess (one call per architecture, sequential, not parallel) instead of a loop within one kernel/process. Each subprocess gets a genuinely fresh GPU context (guaranteed by process exit, unlike `clear_session()`), builds its own datasets (fully determined by `settings`, no state needs to cross the process boundary), trains, and writes both the `.keras` checkpoint (via the existing `get_callbacks`, unchanged paths) and a `history.json` (since a subprocess can't return the `History.history` dict in memory — the calling notebook reads it back from disk). `020_training.ipynb`, `021_training_nll.ipynb`, `023_training_variants.ipynb` all updated to shell out to it via `subprocess.run(..., check=True)` per architecture instead of calling `get_model`/`compile_model`/`model.fit()` directly.

### 7.2. `tf.image.ssim_multiscale` gradient singularity (unrelated to tensorflow-metal)

Even with §7 and §7.1 fixed, `attention_unet` still went `NaN` — reproduced alone, first, in a brand-new process, at batch 2. Not a Metal/session-state bug: pinpointed with `tf.debugging.enable_check_numerics()`, which caught `+Inf` in the output of a `Pow` op whose exponents were `[-0.9552, -0.7144, -0.6999, -0.7637, -0.8667]` — exactly `power_factor - 1` for MS-SSIM's standard power factors `[0.0448, 0.2856, 0.3001, 0.2363, 0.1333]`. This is the *gradient* of `x**p` (`d/dx = p·x^(p-1)`) evaluated at `x=0`: `tf.image.ssim_multiscale` clamps a per-scale structural term to exactly zero internally (via `relu`) to keep the forward pass defined, but the backward pass then computes `0**(negative number) = +Inf` — a real numerical singularity in TF's implementation, unrelated to Apple/Metal, that just happens to be more likely to fire for `attention_unet`'s particular activation statistics than for the other two architectures (in principle it could hit any of them, given the right batch).

**Considered, rejected**: reverting `ms_ssim_loss` to single-scale `tf.image.ssim` would eliminate the bug structurally (no fractional `Pow` at all in single-scale SSIM's formula — just squares and additive stabilizing constants `C1`/`C2`), but would also undo the reason #9 upgraded to MS-SSIM in the first place (the single-scale `σ` trade-off `fixing.md` §4 describes) and move away from the paper's own best-performing (`Mix`) configuration. Rejected in favor of keeping MS-SSIM and neutralizing the singularity instead.

**Fix implemented**: `Adam(clipvalue=settings.GRADIENT_CLIP_VALUE)` (default `1.0`), added to both `compile_model` and `compile_model_nll`. Per-element `clipvalue` (not `clipnorm`) is required: `clipnorm` computes a global gradient norm first, and `Inf` in that computation propagates as `Inf/Inf = NaN` rather than clipping anything; `clipvalue` clips each gradient entry directly via `tf.clip_by_value`, turning `Inf` into a large-but-finite number before it reaches the weight update. Confirmed: `attention_unet` retrained alone with the fix, same conditions that previously went `NaN` at batch 2, completed 2 clean epochs (loss 0.55 → 0.43, val_loss 0.42 → 0.38, ssim 0.31 → 0.54).

**Combined status**: all three fixes (§7 `_ReLUFix`, §7.1 subprocess-per-architecture, §7.2 `clipvalue`) verified together — `resunet`, `unet`, `attention_unet` trained sequentially via `scripts/train_single.py` subprocesses, 1 epoch each, zero `NaN` across all three. 235/235 unit tests pass. `020_training.ipynb`/`021_training_nll.ipynb`/`023_training_variants.ipynb` updated to the subprocess pattern; not yet run end-to-end to full convergence (`EARLY_STOPPING_PATIENCE=20`) for all 8 Round 1 checkpoints.

---

## 8. References

- `theory-links.md` §8.4-8.10 — the ranked findings this plan resolves, and the lecture citations behind each (L03 slide 44/57, L05 slides 19/48/78/81-83, L06 slides 77-78, L07 slides 23/35/37).
- `theory-links.md` §3.5 ("BatchNorm: one slide, four hits, one real problem"), §5.2 ("Weight decay: absent, and unremarked"), §5.5 ("Weight initialization: the wrong default for our activation") — the fuller per-lecture discussion behind findings #1-#3.
- Wu, Y., & He, K. (2018). *Group Normalization*. ECCV 2018 — motivates GroupNorm as a batch-size-independent alternative to BatchNorm for vision models, cited in `theory-links.md` §8.4 as the untried remedy.
- He, K., Zhang, X., Ren, S., & Sun, J. (2015). *Delving Deep into Rectifiers: Surpassing Human-Level Performance on ImageNet Classification*. ICCV 2015 — introduces the He/Kaiming initializer for ReLU networks, the fix for finding #3.
- He, K., Zhang, X., Ren, S., & Sun, J. (2016). *Deep Residual Learning for Image Recognition*. CVPR 2016 — the original ResNet paper; `theory-links.md` §8.9 notes the projection-shortcut variant it describes is meant only for layers where channel counts change, not unconditionally as in `resunet.py`.
- `code-review.md` §3-4 — existing open-issues list; per `theory-links.md` §8.4/§8.10, findings #1 and #6 are not yet folded in there (tracked as Next steps in `.claude/handoff/HANDOFF.md`).
- `evaluation.md` §4b — records the single-artwork test fold instance cited as evidence for finding #4 (no cross-validation).
- [tensorflow-metal ReLU activation fails to clip negative values on M4 Apple Silicon](https://developer.apple.com/forums/thread/818015) — Apple Developer Forums, open/unresolved as of writing; the tensorflow-metal bug behind §7's Round 1 retraining `NaN` failures and the `_ReLUFix` workaround in `scripts/norm_utils.py`.
- Zhao, H., Gallo, O., Frosio, I., & Kautz, J. (2016). *Loss Functions for Image Restoration with Neural Networks*. IEEE Transactions on Computational Imaging (arXiv:1511.08861) — motivates findings #9/#10: `ℓ2` correlates poorly with perceived quality and produces flat-region artifacts; `ℓ1` alone already beats it; the best combination tested is `0.84·MS-SSIM + 0.16·ℓ1` (`Mix`). Read in full from `biblio/1511.08861v3.pdf`.
- Seitzer, M., et al. (2022). *On the Pitfalls of Heteroscedastic Uncertainty Estimation with Probabilistic Neural Networks*. ICLR 2022 (arXiv:2203.09168) — origin of the beta-reweighting trick `#10` generalizes from the Gaussian variance to the Laplace scale; already cited for the existing `beta_gaussian_nll_loss` in `code-review.md` §7.6.
- `.claude/handoff/HANDOFF.md` — session context this plan was derived from; lists the same 8 findings as "Top priority" before this document existed; also records the gaussian-vs-beta NLL comparison that #10 deliberately closes.
